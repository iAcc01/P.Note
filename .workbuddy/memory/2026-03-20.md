# 2026-03-20 工作日志

## PromptNote 全站代码修复

根据之前的代码审查报告，修复了 6 大类 20+ 处问题：

1. **CSS 硬编码值替换为变量**：`#FFFFFF` → `var(--color-bg-card)`，`rgba(...)` shadow → `var(--shadow-sm/lg)`，滚动条颜色抽为 `--color-scrollbar-thumb` 变量
2. **删除 mockData.ts 无用 React import**
3. **Sidebar 合并重复**：`navIcon`/`navIconCollapsed` 合一，4 处重复 hover 回调提取为 `hoverHandlers()` 辅助函数
4. **MyCreations getTabStyle 简化**：active/hovered 返回相同样式 → 合并为 `highlighted` 分支
5. **14 处 borderRadius 硬编码改为 CSS 变量引用**（`--radius-md/lg/xl/full`）
6. **其他**：PromptCard `as any` 移除、StatusBar 冗余 spacer 删除、PlaceholderPage icon 联合类型、App.tsx 响应式 padding

验证：TSC 0 errors, Lint 0 errors
